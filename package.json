{
  "name": "nrds-server",
  "version": "1.0.0",
  "description": "Basic server app using GFC to accompany the NRDS Client app",
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test server/tests/*.ts"
  },
  "devDependencies": {
    "aws-cdk-lib": "^2.100.0",
    "constructs": "^10.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "@types/aws-lambda": "^8.10.0",
    "ts-node": "^10.9.1",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  }
}
